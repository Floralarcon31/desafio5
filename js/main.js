
//PRUEBA
/*
class paquete{
    constructor(destino, id, precio, butacas){
        this.destino=destino;
        this.id =id;
        this.precio=precio;
        this.butacas=butacas;
    }
   
    ultimasButacas(cantX){
        if(cantX <= this.butacas) {
            this.butacas -= cantX;
        }
        
    }
}

const paquete2 = new paquete("Rio de Janeiro",55, 800,18);


let cantX = prompt("Indique cuantas butacas necesita")
paquete2.ultimasButacas(cantX);

console.log(`Ultimas ${paquete2.butacas} butacas.No te quedes sin tu lugar!`);

*/

/*

let eleccion = prompt ("Elije uno de estos 3 destinos Nacionales - IGZ - MDZ - FTE").toLocaleUpperCase();

if(eleccion == "IGZ" && "MDZ" && "FTE"){

    let formaDePago = prompt("Elije una forma de Pago - EFECTIVO - TC - TRANSFERENCIA ").toLocaleUpperCase();
    if (formaDePago == "EFECTIVO"){
        return this.precio - (this.precio * 0,2);
    }
    else if (formaDePago == "TC"){
        console.log("No hay descuento con tarjeta de credito");
    }
    else if ( formaDePago == "TRANSFERENCIA"){
        return this.precio - (this.precio * 0,1);
    }
    else{
        console.log("No contamos con ese medio de pago, Intentelo otra vez");
    }


}else if (eleccion != "IGZ" || "MDZ" || "FTE"){

}


*/
